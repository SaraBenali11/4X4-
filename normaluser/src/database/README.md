Database package (moved out of backend).

This folder contains the SQLAlchemy models and seed data shared by the backend.

Files:

- `models.py` - SQLAlchemy models and `db` instance
- `data.py` - seed/sample data used for development
- `datatables.py` - helper to create tables and seed data
