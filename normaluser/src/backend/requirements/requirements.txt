# Python Dependencies for Flask Backend
# Install these packages using: pip install -r requirements.txt

# Flask + Extensions
Flask==2.3.3
flask-cors==4.0.0
Flask-SQLAlchemy==3.0.5
PyMySQL==1.1.0
python-dotenv==1.0.0
# Added for integration tests
requests==2.31.0
pytest==7.4.0
# Note: On Windows or other environments, use PyMySQL or mysql-connector-python instead of mysqlclient/MySQLdb
